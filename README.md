# nix-core
core nix modules for all my servers

## Modules Include
- 🔥 Promethues Exporter: all the stats
- 🐳 Docker: everything is a container these days
- 📈 Beszel Agent: more stats
- 🕝 Common server setup: base user, ssh, some common packages, ...

## Example Flake
```nix
{
  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-25.11";
    nix-core = {
      url = "github:chusovich/nix-core";
      inputs.nixpkgs.follows = nixpkgs;
    }
  };

  outputs = { self, nixpkgs, nix-core, ... }: {
    nixosConfigurations.myhost = nixpkgs.lib.nixosSystem {
      system = "x86_64-linux";

      modules = [
        nix-core.modules.default
        # or pick individual ones:
        # nix-core.modules.beszel
      ];
    };
  };
}
```

### Example Beszel Environment File
```yaml
# Port the agent listens on
# LISTEN=45876 # this is the default port, change if desired

# SSH public key used by the Beszel hub to authenticate
KEY=ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAINnHQnhcqN9EfGocDO0G/jJXOGvJ5bS/fHLXGrbxvGxn

# Agent token (generated by the Beszel hub)
TOKEN=b826dad4-17d1-49ba-b830-409f28067d99

# Optional: extra filesystem mounts to monitor (colon-separated)
# EXTRA_FILESYSTEMS=/extra-filesystems/sda1:/extra-filesystems/sdb1
```
